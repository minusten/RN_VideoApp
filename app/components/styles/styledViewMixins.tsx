import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    styledViewMixins: {
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center',
    },
    color: {
        color: '#fff',
    },
});

